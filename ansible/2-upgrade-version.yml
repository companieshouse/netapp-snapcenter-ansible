---
- import_playbook: ansible-ssh-key.yml

- name: Upgrade NetApp SnapCenter Version
  hosts: aws_ec2
  remote_user: ec2-user
  become: true
  gather_facts: true
  roles:
    - role: ec2_tags
    - role: vault
    - role: snapcenter_upgrade
    # - role: companieshouse.security.security_scans
